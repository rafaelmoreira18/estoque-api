import {PrismaClient} from '@prisma/client'

const prismaClient = new PrismaClient();

export default prismaClient;


//interact with the database in other parts of the application.
//
//instância para interagir com o banco de dados em outras partes da aplicação.